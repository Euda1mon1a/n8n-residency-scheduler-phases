{
  "mcpServers": {
    "n8n": {
      "command": "npx",
      "args": [
        "-y",
        "supergateway",
        "--streamableHttp",
        "${N8N_MCP_URL}",
        "--header",
        "authorization:Bearer ${N8N_MCP_TOKEN}"
      ]
    }
  }
}
